#pragma once

/*
 * WiFi credentials for the STA link (ticket 02 MVP: hardcoded).
 * Copy this file to wifi_config.h and fill in your 2.4GHz WiFi SSID and
 * password. wifi_config.h is gitignored — never commit real credentials.
 * TODO(ticket 05+): move to SoftAP provisioning portal + NVS persistence.
 */

#define WIFI_SSID          "YOUR_WIFI_SSID"
#define WIFI_PASS          "YOUR_WIFI_PASSWORD"
